# Plan 04-02: Frontend — Páginas de conversaciones y llamadas

## Tasks

### T1: Página /conversations

`apps/web/app/(dashboard)/conversations/page.tsx`

```tsx
// Lista de conversaciones con tabs WhatsApp / Voz / Todas
// Columnas: Deudor, Canal, Último mensaje, Estado, Fecha, Acciones
// Badge "ESCALADA" en rojo para conversations.status === 'escalated'
// Click → /conversations/[id]
```

### T2: Página /conversations/[id]

`apps/web/app/(dashboard)/conversations/[id]/page.tsx`

```tsx
// Hilo tipo chat con mensajes
// MessageBubble: 
//   - direction='out' → burbuja derecha (coral #D85A30)
//   - direction='in' → burbuja izquierda (gris)
//   - Si human_sent → icono de persona junto a la burbuja
// ReplyInput (solo visible si channel === 'whatsapp'):
//   - textarea + botón Enviar
//   - POST /api/v1/conversations/:id/reply
//   - Toast de confirmación
// Si status === 'escalated': banner amarillo "Requiere atención humana"
```

### T3: Página /calls

`apps/web/app/(dashboard)/calls/page.tsx`

```tsx
// Lista de contacts con channel='voice'
// Columnas: Deudor, Fecha, Duración, Outcome, Transcript
// TranscriptViewer: accordion que muestra el transcript de la llamada
// Filtros: Fecha, Outcome (answered / no_answer / promise_to_pay)
```

### T4: Badge escalaciones en Sidebar

`apps/web/components/shared/Sidebar.tsx`

```tsx
// Agregar item "Conversaciones" con badge de escalaciones pendientes
// useQuery para contar escalations activas, polling cada 30s
// Badge rojo con número si > 0
```

### T5: KPIs en dashboard

En `/dashboard/page.tsx` agregar dos KPI cards nuevas:
- **Tasa promesa WA**: (debts con promise_to_pay vía WA / total contactados por WA) × 100
- **Atención llamada**: (llamadas con outcome='answered' / total llamadas) × 100

Calcular desde el endpoint de stats existente o nuevo endpoint GET /contacts/stats.

### T6: Componentes

`apps/web/components/conversations/MessageBubble.tsx`
```tsx
interface Props {
  direction: 'in' | 'out';
  content: string;
  sentAt: string;
  humanSent?: boolean;
  channel: string;
}
```

`apps/web/components/calls/TranscriptViewer.tsx`
```tsx
interface Props {
  transcript: string | null;
  summary?: string | null;
  callId: string;
}
// Collapsable con botón "Ver transcript"
// Muestra texto con scroll si > 200 chars
```

### T7: Tests
- `MessageBubble.test.tsx` — renderiza correctamente in/out, badge humano
- `TranscriptViewer.test.tsx` — colapsa/expande, null transcript
- E2E: `/conversations` carga lista (mock API)

## Acceptance criteria
- [ ] `/conversations` muestra lista de conversaciones
- [ ] `/conversations/[id]` muestra hilo y permite respuesta manual WA
- [ ] Escalaciones con badge visible en sidebar
- [ ] `/calls` muestra lista con transcripts colapsables
- [ ] KPIs WA y Voice en dashboard
- [ ] Tests de componentes pasan
