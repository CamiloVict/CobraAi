# Plan 02-01: VapiVoiceAdapter — Llamadas outbound reales

## Objetivo
Implementar `VapiVoiceAdapter` que reemplaza el stub usando la REST API de Vapi.

## Tasks

### T1: Agregar cliente HTTP para Vapi
No hay SDK oficial de Vapi para Node; usar `axios` o `fetch` nativo.
Vapi tiene `@vapi-ai/server-sdk` — verificar disponibilidad:
```bash
pnpm add @vapi-ai/server-sdk --filter=service-notifications
```
Si no existe o es inestable, usar axios directamente.

### T2: System prompt del agente Vapi (configurar en dashboard)
```
Eres un agente de cobranza de CobraAI en Colombia. Tu nombre es "Valeria".

PERSONALIDAD:
- Amable, empática, profesional
- Nunca amenazante ni agresiva
- Directa pero respetuosa
- Hablas español colombiano neutro

OBJETIVO:
Recordar al cliente su saldo pendiente y obtener una promesa de pago o acordar un plan.

FLUJO:
1. Preséntate: "Buenos días/tardes, soy Valeria de CobraAI, llamo en representación de [empresa]."
2. Verifica identidad: "¿Hablo con [nombre]?"
3. Informa saldo: "Le contactamos porque tiene un saldo pendiente de $[monto] con vencimiento [fecha]."
4. Escucha y responde según su reacción.
5. Si promete pagar: confirma fecha y monto, agradece.
6. Si pide plan de pago: explica opciones (3 cuotas), ofrece enviar enlace por WhatsApp.
7. Si disputa la deuda: anota la disputa, ofrece comunicar al área de atención.
8. Si no puede pagar ahora: pregunta cuándo podría hacerlo.

REGLAS LEGALES (Ley 1266 Colombia):
- NO amenazar con acciones legales inexistentes
- NO contactar terceros sin autorización
- NO llamar fuera de 6am-10pm hora Colombia
- Identificar SIEMPRE la empresa acreedora
- Respetar solicitud de no contacto

VARIABLES DISPONIBLES:
- {nombre}: nombre del deudor
- {monto}: monto pendiente
- {empresa}: nombre de la empresa acreedora
- {fecha_vencimiento}: fecha de vencimiento
- {link_pago}: enlace de pago

CIERRE:
- Si resuelto: "Muchas gracias por su tiempo. Que tenga un excelente día."
- Si sin resolución: "Entiendo. Le haremos llegar información adicional. ¿Le parece bien?"
```

### T3: Crear VapiVoiceAdapter
Archivo: `apps/service-notifications/src/adapters/vapi-voice.adapter.ts`

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import axios from 'axios';
import type {
  VoiceAgentPort,
  InitiateCallInput,
  InitiateCallResult,
  CallStatus,
} from '@cobrai/ports';

interface VapiCallResponse {
  id: string;
  status: string;
}

@Injectable()
export class VapiVoiceAdapter implements VoiceAgentPort {
  private readonly logger = new Logger(VapiVoiceAdapter.name);
  private readonly baseUrl = 'https://api.vapi.ai';
  private readonly apiKey: string;
  private readonly agentId: string;

  constructor(private readonly config: ConfigService) {
    this.apiKey = config.getOrThrow<string>('VAPI_API_KEY');
    this.agentId = config.getOrThrow<string>('VAPI_AGENT_ID');
  }

  async initiateCall(input: InitiateCallInput): Promise<InitiateCallResult> {
    const ctx = input.strategy_context;
    const phone = input.debtor_phone.startsWith('+')
      ? input.debtor_phone
      : `+${input.debtor_phone}`;

    try {
      const response = await axios.post<VapiCallResponse>(
        `${this.baseUrl}/call`,
        {
          assistantId: this.agentId,
          phoneNumberId: undefined, // Vapi usa su propio número Twilio
          customer: {
            number: phone,
            name: ctx.variables.nombre ?? ctx.variables.debtor_name,
          },
          assistantOverrides: {
            variableValues: {
              nombre: ctx.variables.nombre ?? 'cliente',
              monto: ctx.variables.monto ?? ctx.variables.amount ?? '0',
              empresa: ctx.variables.empresa ?? 'CobraAI',
              fecha_vencimiento: ctx.variables.due_date ?? '',
              link_pago: ctx.variables.link_pago ?? ctx.variables.payment_link ?? '',
            },
          },
          metadata: {
            debt_id: input.debt_id,
            tenant_id: ctx.tenant_id,
            strategy_id: ctx.strategy_id,
          },
        },
        {
          headers: {
            Authorization: `Bearer ${this.apiKey}`,
            'Content-Type': 'application/json',
          },
        },
      );

      this.logger.log(`Llamada Vapi iniciada call_id=${response.data.id} → ${phone}`);
      return { call_id: response.data.id, status: 'queued' };
    } catch (err: unknown) {
      this.logger.error(`Vapi call fallida → ${phone}: ${String(err)}`);
      return { call_id: '', status: 'failed' };
    }
  }

  async getCallStatus(call_id: string): Promise<CallStatus> {
    try {
      const response = await axios.get<VapiCallResponse>(
        `${this.baseUrl}/call/${call_id}`,
        { headers: { Authorization: `Bearer ${this.apiKey}` } },
      );
      const vapiStatus = response.data.status;
      const mapped = this.mapStatus(vapiStatus);
      return { call_id, status: mapped };
    } catch {
      return { call_id, status: 'queued' };
    }
  }

  private mapStatus(vapiStatus: string): 'queued' | 'ringing' | 'in-progress' | 'completed' | 'failed' | 'no-answer' | 'busy' {
    switch (vapiStatus) {
      case 'queued': return 'queued';
      case 'ringing': return 'ringing';
      case 'in-progress': return 'in-progress';
      case 'ended': return 'completed';
      case 'failed': return 'failed';
      default: return 'queued';
    }
  }
}
```

### T4: Actualizar adapters.module.ts
```typescript
import { VapiVoiceAdapter } from './vapi-voice.adapter';
// Cambiar VoiceAdapter → VapiVoiceAdapter en providers y exports
// Y en contacts.module.ts si está importado directamente
```

### T5: Unit tests
Archivo: `apps/service-notifications/src/adapters/vapi-voice.adapter.spec.ts`

```typescript
vi.mock('axios');

describe('VapiVoiceAdapter', () => {
  it('initiateCall → llama API Vapi y retorna call_id', ...);
  it('initiateCall fallida → retorna status: failed sin lanzar', ...);
  it('getCallStatus → mapea status ended → completed', ...);
  it('phone sin + → agrega + al número', ...);
});
```

### T6: Actualizar .env.example
```
# Vapi.ai Voice Agent
VAPI_API_KEY=vapi_xxxxxxxxxxxx
VAPI_AGENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
VAPI_WEBHOOK_SECRET=
```

## Acceptance criteria
- [ ] `VapiVoiceAdapter` implementa `VoiceAgentPort` (TypeScript verifica)
- [ ] `initiateCall()` hace POST a `https://api.vapi.ai/call`
- [ ] `getCallStatus()` hace GET a `https://api.vapi.ai/call/:id`
- [ ] Fallo de Vapi → `{ status: 'failed' }` sin lanzar excepción
- [ ] Tests pasan con axios mockeado
- [ ] Build sin errores
