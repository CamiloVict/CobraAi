# Plan 03-01: ConversationAgentService — LLM con contexto de deuda

## Objetivo
Servicio que usa GPT-4o-mini para generar respuestas contextualizadas a mensajes de WhatsApp.

## Tasks

### T1: Instalar openai SDK
```bash
pnpm add openai --filter=service-notifications
```

### T2: System prompt
Archivo: `apps/service-notifications/src/agent/prompts/cobrai-system.prompt.ts`

```typescript
export function buildSystemPrompt(context: {
  debtorName: string;
  companyName: string;
  amount: string;
  currency: string;
  dueDate: string;
  paymentLink: string;
  debtStatus: string;
}): string {
  return `Eres Valeria, agente de cobranza de CobraAI, representando a ${context.companyName}.
Hablas español colombiano de manera amable, profesional y empática.
NUNCA eres agresiva, amenazante ni usas lenguaje que pueda infringir la Ley 1266 de Colombia.

CONTEXTO DEL DEUDOR:
- Nombre: ${context.debtorName}
- Saldo pendiente: ${context.currency} ${context.amount}
- Fecha vencimiento: ${context.dueDate}
- Estado actual: ${context.debtStatus}
- Link de pago: ${context.paymentLink}

TU OBJETIVO: Ayudar al deudor a resolver su situación de la manera más conveniente para ambas partes.

REGLAS:
1. Siempre responde en español colombiano
2. Respuestas cortas (máximo 3 oraciones para WhatsApp)
3. Si el deudor promete pagar: confirma fecha y monto, agradece
4. Si pide plan de pagos: ofrece dividir en hasta 3 cuotas, envía el link
5. Si disputa la deuda: anota que revisarás, ofrece comunicar al área de atención
6. Si dice que ya pagó: agradece, explica que el pago puede tomar 24-48h en reflejarse
7. Si es agresivo: mantén la calma, ofrece escalar a un humano
8. Si pide no ser contactado: respeta y confirma que no se le contactará más

FORMATO DE RESPUESTA (JSON):
{
  "intent": "promise_to_pay" | "dispute" | "plan_request" | "escalate_human" | "payment_confirmed" | "opt_out" | "unrelated",
  "response": "texto de respuesta para el deudor",
  "promise_date": "YYYY-MM-DD si prometió pagar" | null,
  "promise_amount": número | null
}`;
}
```

### T3: ConversationAgentService
Archivo: `apps/service-notifications/src/agent/conversation-agent.service.ts`

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import OpenAI from 'openai';
import { PrismaService } from '@cobrai/db';
import { KafkaService } from '../kafka/kafka.service';
import { TwilioWhatsAppAdapter } from '../adapters/twilio-whatsapp.adapter';
import { buildSystemPrompt } from './prompts/cobrai-system.prompt';

interface AgentResponse {
  intent: string;
  response: string;
  promise_date?: string | null;
  promise_amount?: number | null;
}

@Injectable()
export class ConversationAgentService {
  private readonly logger = new Logger(ConversationAgentService.name);
  private readonly openai: OpenAI;
  private readonly model: string;
  private readonly maxTokens: number;

  constructor(
    private readonly config: ConfigService,
    private readonly prisma: PrismaService,
    private readonly kafka: KafkaService,
    private readonly whatsapp: TwilioWhatsAppAdapter,
  ) {
    this.openai = new OpenAI({ apiKey: config.getOrThrow('OPENAI_API_KEY') });
    this.model = config.get('OPENAI_MODEL') ?? 'gpt-4o-mini';
    this.maxTokens = Number(config.get('OPENAI_MAX_TOKENS') ?? 500);
  }

  async processInboundMessage(payload: {
    debtor_id: string;
    tenant_id: string;
    conversation_id: string;
    phone: string;
    body: string;
  }): Promise<void> {
    const { debtor_id, tenant_id, conversation_id, phone, body } = payload;

    // 1. Cargar contexto
    const [debtor, conversation] = await Promise.all([
      this.prisma.debtor.findFirst({
        where: { id: debtor_id, tenantId: tenant_id },
        include: {
          debts: {
            where: { tenantId: tenant_id, deletedAt: null, status: { notIn: ['paid_full', 'written_off'] } },
            orderBy: { amountOutstanding: 'desc' },
            take: 1,
          },
        },
      }),
      this.prisma.conversation.findUnique({ where: { id: conversation_id } }),
    ]);

    if (!debtor || !conversation) {
      this.logger.warn(`Contexto no encontrado para deudor ${debtor_id}`);
      return;
    }

    const debt = debtor.debts[0];
    if (!debt) {
      this.logger.warn(`No hay deuda activa para deudor ${debtor_id}`);
      return;
    }

    // 2. Cargar historial de mensajes (últimos 20)
    const messages = await this.prisma.message.findMany({
      where: { conversationId: conversation_id, deletedAt: null },
      orderBy: { sentAt: 'desc' },
      take: 20,
    });
    const history = messages.reverse();

    // 3. Construir mensajes para OpenAI
    const systemPrompt = buildSystemPrompt({
      debtorName: debtor.name,
      companyName: 'CobraAI Demo',
      amount: String(debt.amountOutstanding),
      currency: debt.currency,
      dueDate: new Date(debt.dueDate).toLocaleDateString('es-CO'),
      paymentLink: `${this.config.get('PAYMENT_LINK_BASE_URL') ?? 'http://localhost:3001/pay'}/${debt.id}`,
      debtStatus: debt.status,
    });

    const chatMessages: OpenAI.Chat.ChatCompletionMessageParam[] = [
      { role: 'system', content: systemPrompt },
      ...history.map((m) => {
        const parsed = this.parseContent(m.content);
        return {
          role: m.direction === 'out' ? ('assistant' as const) : ('user' as const),
          content: parsed.text ?? parsed.body ?? m.content,
        };
      }),
      { role: 'user', content: body },
    ];

    // 4. Llamar a OpenAI
    let agentResponse: AgentResponse;
    try {
      const completion = await this.openai.chat.completions.create({
        model: this.model,
        messages: chatMessages,
        max_tokens: this.maxTokens,
        response_format: { type: 'json_object' },
        temperature: 0.7,
      });
      const raw = completion.choices[0]?.message?.content ?? '{}';
      agentResponse = JSON.parse(raw) as AgentResponse;
    } catch (err) {
      this.logger.error(`OpenAI error: ${String(err)}`);
      agentResponse = {
        intent: 'unrelated',
        response: 'Gracias por su mensaje. Un agente se comunicará con usted pronto.',
      };
    }

    this.logger.log(`Intent detectado: ${agentResponse.intent} para deudor ${debtor_id}`);

    // 5. Guardar respuesta en BD
    await this.prisma.message.create({
      data: {
        tenantId: tenant_id,
        conversationId: conversation_id,
        direction: 'out',
        channel: 'whatsapp',
        content: JSON.stringify({ text: agentResponse.response }),
        status: 'sent',
        sentAt: new Date(),
      },
    });

    // 6. Enviar respuesta por WhatsApp
    if (agentResponse.intent !== 'opt_out') {
      await this.whatsapp.sendTemplate({
        to: phone,
        template_id: 'agent_response',
        variables: { body: agentResponse.response },
        tenant_id,
      });
    }

    // 7. Acciones según intent
    await this.handleIntent(agentResponse, { tenant_id, debt_id: debt.id, debtor_id, conversation_id });
  }

  private async handleIntent(
    response: AgentResponse,
    ctx: { tenant_id: string; debt_id: string; debtor_id: string; conversation_id: string },
  ): Promise<void> {
    switch (response.intent) {
      case 'promise_to_pay':
        await this.prisma.debt.updateMany({
          where: { id: ctx.debt_id, tenantId: ctx.tenant_id },
          data: { status: 'promised' },
        });
        if (response.promise_date) {
          await this.prisma.promiseToPay.create({
            data: {
              tenantId: ctx.tenant_id,
              debtId: ctx.debt_id,
              amount: response.promise_amount ?? 0,
              promisedDate: new Date(response.promise_date),
              status: 'pending',
            },
          });
        }
        await this.kafka.publish('cobrai.debt.promise_registered', ctx.tenant_id, {
          debt_id: ctx.debt_id,
          channel: 'whatsapp',
          promise_date: response.promise_date,
          promise_amount: response.promise_amount,
        });
        break;

      case 'dispute':
        await this.prisma.debt.updateMany({
          where: { id: ctx.debt_id, tenantId: ctx.tenant_id },
          data: { status: 'disputed' },
        });
        await this.kafka.publish('cobrai.debt.disputed', ctx.tenant_id, {
          debt_id: ctx.debt_id,
          channel: 'whatsapp',
        });
        break;

      case 'escalate_human':
        await this.prisma.conversation.update({
          where: { id: ctx.conversation_id },
          data: { status: 'escalated' },
        });
        await this.kafka.publish('cobrai.escalation.requested', ctx.tenant_id, {
          debt_id: ctx.debt_id,
          debtor_id: ctx.debtor_id,
          channel: 'whatsapp',
          reason: 'deudor_solicito_humano',
        });
        break;

      case 'opt_out':
        await this.prisma.contactConsent.updateMany({
          where: { tenantId: ctx.tenant_id, debtorId: ctx.debtor_id, channel: 'whatsapp', revokedAt: null },
          data: { revokedAt: new Date() },
        });
        break;

      default:
        // unrelated, plan_request, payment_confirmed — solo registrado en BD
        break;
    }
  }

  private parseContent(content: string): Record<string, string> {
    try {
      return JSON.parse(content) as Record<string, string>;
    } catch {
      return { text: content };
    }
  }
}
```

### T4: Kafka Consumer
Archivo: `apps/service-notifications/src/agent/conversation-agent.consumer.ts`

```typescript
import { Injectable, OnModuleInit, Logger } from '@nestjs/common';
import { KafkaService } from '../kafka/kafka.service';
import { ConversationAgentService } from './conversation-agent.service';

@Injectable()
export class ConversationAgentConsumer implements OnModuleInit {
  private readonly logger = new Logger(ConversationAgentConsumer.name);

  constructor(
    private readonly kafka: KafkaService,
    private readonly agent: ConversationAgentService,
  ) {}

  async onModuleInit(): Promise<void> {
    await this.kafka.subscribe(
      'cobrai.whatsapp.message_received',
      'agent-wa-consumer',
      async (payload) => {
        try {
          await this.agent.processInboundMessage(payload);
        } catch (err) {
          this.logger.error(`Agent consumer error: ${String(err)}`);
        }
      },
    );
  }
}
```

### T5: AgentModule
Archivo: `apps/service-notifications/src/agent/agent.module.ts`

```typescript
import { Module } from '@nestjs/common';
import { ConversationAgentService } from './conversation-agent.service';
import { ConversationAgentConsumer } from './conversation-agent.consumer';
import { KafkaModule } from '../kafka/kafka.module';
import { PrismaModule } from '../prisma/prisma.module';
import { AdaptersModule } from '../adapters/adapters.module';

@Module({
  imports: [KafkaModule, PrismaModule, AdaptersModule],
  providers: [ConversationAgentService, ConversationAgentConsumer],
})
export class AgentModule {}
```

Importar `AgentModule` en `app.module.ts`.

### T6: Unit tests
Archivo: `apps/service-notifications/src/agent/conversation-agent.service.spec.ts`

Tests (con OpenAI mockeado):
- `processInboundMessage` con intent `promise_to_pay` → debt.status = 'promised' + PromiseToPay creado
- Intent `dispute` → debt.status = 'disputed'
- Intent `escalate_human` → conversation.status = 'escalated'
- Intent `opt_out` → contactConsent revocado, NO envío WA
- OpenAI falla → respuesta genérica, sin error lanzado
- Historial de 20+ mensajes → solo los últimos 20 incluidos

## Acceptance criteria
- [ ] Mensaje WA inbound → GPT responde → deudor recibe en WA
- [ ] `promise_to_pay` → debt.status = 'promised' en BD
- [ ] `escalate_human` → conversation.status = 'escalated'
- [ ] `opt_out` → consent revocado, no respuesta enviada
- [ ] OpenAI mock: tests pasan sin llamada real
- [ ] Env vars documentadas en `.env.example`
