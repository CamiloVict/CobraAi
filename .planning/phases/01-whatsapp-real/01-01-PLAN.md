# Plan 01-01: TwilioWhatsAppAdapter — Envío real

## Objetivo
Implementar `TwilioWhatsAppAdapter` que reemplaza el stub actual.

## Tasks

### T1: Instalar dependencia Twilio
```bash
pnpm add twilio --filter=service-notifications
```
Verificar que `twilio` aparece en `apps/service-notifications/package.json`.

### T2: Crear TwilioWhatsAppAdapter
Archivo: `apps/service-notifications/src/adapters/twilio-whatsapp.adapter.ts`

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import twilio from 'twilio';
import type { WhatsAppPort, SendWhatsAppTemplateInput, SendWhatsAppTemplateResult } from '@cobrai/ports';
import { PrismaService } from '@cobrai/db';

@Injectable()
export class TwilioWhatsAppAdapter implements WhatsAppPort {
  private readonly logger = new Logger(TwilioWhatsAppAdapter.name);
  private readonly client: twilio.Twilio;
  private readonly from: string;

  constructor(
    private readonly config: ConfigService,
    private readonly prisma: PrismaService,
  ) {
    this.client = twilio(
      config.getOrThrow<string>('TWILIO_ACCOUNT_SID'),
      config.getOrThrow<string>('TWILIO_AUTH_TOKEN'),
    );
    this.from = config.getOrThrow<string>('TWILIO_WA_FROM'); // whatsapp:+1415...
  }

  async sendTemplate(input: SendWhatsAppTemplateInput): Promise<SendWhatsAppTemplateResult> {
    // Normalizar número a formato WhatsApp
    const to = input.to.startsWith('whatsapp:') ? input.to : `whatsapp:${input.to}`;

    // Renderizar el body con variables (templates simples en sandbox)
    const body = this.renderBody(input.template_id, input.variables);

    try {
      const msg = await this.client.messages.create({
        from: this.from,
        to,
        body,
      });
      this.logger.log(`WA enviado SID=${msg.sid} to=${to}`);
      return { message_id: msg.sid, status: 'sent' };
    } catch (err: unknown) {
      this.logger.error(`WA fallido to=${to}: ${String(err)}`);
      return { message_id: '', status: 'failed' };
    }
  }

  async isOptedIn(phone: string, tenant_id: string): Promise<boolean> {
    // Consultar consent en BD (ya gestionado por ComplianceService)
    const consent = await this.prisma.contactConsent.findFirst({
      where: {
        tenantId: tenant_id,
        debtor: { phones: { path: '$[*]', string_contains: phone.replace('whatsapp:', '') } },
        channel: 'whatsapp',
        revokedAt: null,
        deletedAt: null,
      },
    });
    return !!consent;
  }

  private renderBody(templateId: string, variables: Record<string, string>): string {
    // Mapeo de template_id → mensaje. En producción usar content_sid de Twilio.
    // Para sandbox cualquier texto libre funciona.
    const nombre = variables.nombre ?? variables.debtor_name ?? 'estimado cliente';
    const monto = variables.monto ?? variables.amount ?? '';
    const link = variables.link_pago ?? variables.link ?? '';

    switch (true) {
      case templateId.includes('recordatorio'):
        return `Hola ${nombre}, le recordamos que tiene un saldo pendiente de $${monto}. Puede pagarlo aquí: ${link}`;
      case templateId.includes('plan_pago'):
        return `Hola ${nombre}, tenemos una propuesta de plan de pago para su saldo de $${monto}. Contáctenos para más información.`;
      case templateId.includes('confirmacion'):
        return `Hola ${nombre}, confirmamos recepción de su pago. ¡Gracias!`;
      default:
        return `Hola ${nombre}, le contactamos de CobraAI sobre su cuenta. Saldo: $${monto}. Info: ${link}`;
    }
  }
}
```

### T3: Actualizar adapters.module.ts
Archivo: `apps/service-notifications/src/adapters/adapters.module.ts`

Cambiar el binding de `WhatsAppAdapter` a `TwilioWhatsAppAdapter`:
```typescript
// Antes:
providers: [EmailAdapter, SmsAdapter, WhatsAppAdapter, VoiceAdapter],
exports: [EmailAdapter, SmsAdapter, WhatsAppAdapter, VoiceAdapter],

// Después (cambio de clase, ContactsService ya inyecta por clase concreta):
import { TwilioWhatsAppAdapter } from './twilio-whatsapp.adapter';
providers: [EmailAdapter, SmsAdapter, TwilioWhatsAppAdapter, VoiceAdapter],
exports: [EmailAdapter, SmsAdapter, TwilioWhatsAppAdapter, VoiceAdapter],
```
Actualizar `contacts.module.ts` si importa `WhatsAppAdapter` directamente.

### T4: Actualizar ContactsService para usar TwilioWhatsAppAdapter
Si `contacts.service.ts` inyecta `WhatsAppAdapter` por clase, cambiar la inyección:
```typescript
// Cambiar:
constructor(private readonly whatsapp: WhatsAppAdapter, ...)
// Por:
constructor(private readonly whatsapp: TwilioWhatsAppAdapter, ...)
```
O mejor aún — usar token de inyección basado en interfaz para no acoplarse:
```typescript
// En adapters.module.ts agregar token:
{ provide: 'WhatsAppPort', useClass: TwilioWhatsAppAdapter }
// En contacts.service.ts:
constructor(@Inject('WhatsAppPort') private readonly whatsapp: WhatsAppPort, ...)
```

### T5: Actualizar .env.example
Archivo: `apps/service-notifications/.env.example`
Agregar:
```
# Twilio WhatsApp
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_WA_FROM=whatsapp:+14155238886
```

### T6: Unit tests
Archivo: `apps/service-notifications/src/adapters/twilio-whatsapp.adapter.spec.ts`

Tests a cubrir:
- `sendTemplate()` con número válido → llama `client.messages.create` → retorna `{ message_id, status: 'sent' }`
- `sendTemplate()` cuando Twilio lanza excepción → retorna `{ status: 'failed' }`
- `isOptedIn()` con consent en BD → retorna `true`
- `isOptedIn()` sin consent → retorna `false`
- Template `recordatorio` renderiza mensaje correcto con variables

Mock de Twilio:
```typescript
vi.mock('twilio', () => ({
  default: vi.fn(() => ({
    messages: {
      create: vi.fn().mockResolvedValue({ sid: 'SMtest123' })
    }
  }))
}));
```

## Acceptance criteria
- [ ] `TwilioWhatsAppAdapter` implementa `WhatsAppPort` (TypeScript verifica)
- [ ] `pnpm build --filter=service-notifications` sin errores
- [ ] Todos los unit tests pasan (`pnpm test --filter=service-notifications`)
- [ ] `.env.example` contiene las 3 variables de Twilio WA
